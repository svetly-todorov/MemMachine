# MemMachine Meta-Package

This package is a meta-package that installs the full MemMachine suite, including:

- `memmachine-client`: The Python client library.
- `memmachine-server`: The MemMachine server.

## Installation

```bash
pip install memmachine
```

This will automatically install both the client and server packages.
